# Airflow DAG dependencies
# Installed at container startup via pip install -r /requirements.txt

--prefer-binary
--only-binary=:all:

httpx==0.27.0
psycopg[binary]==3.1.19
pydantic==2.7.1
python-dotenv==1.0.1
tenacity==8.3.0